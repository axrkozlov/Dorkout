<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.axfex.dorkout">

    <application
        android:name=".WorkoutApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.workouts.list.WorkoutsActivity"
            android:label="@string/title_activity_workouts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".views.workouts.addedit.AddEditWorkoutActivity"
            android:label="@string/title_activity_add_edit_workout"/>
        <activity
            android:name=".views.exercises.list.ExercisesActivity"
            android:label="@string/title_activity_exercises"
            android:parentActivityName=".views.workouts.list.WorkoutsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.workouts.list.WorkoutsActivity" />
        </activity>
        <activity
            android:name=".views.exercises.addedit.AddEditExerciseActivity"
            android:label="@string/title_activity_add_edit_exercise"/>
    </application>

</manifest>